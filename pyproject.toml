[project]
name = "sms-formats"
version = "0.1.0"
description = "SMS parsing formats and sender lists"
requires-python = ">=3.8"

[tool.ruff]
target-version = "py38"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]  # pycodestyle errors, Pyflakes, isort
